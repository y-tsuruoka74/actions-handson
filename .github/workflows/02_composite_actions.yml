name: 02 Composite Actions

on:
  workflow_dispatch:

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
    - name: Notify to Slack
      uses: ippanpeople/slack-notify-action@v0.0.4
      with:
        webhook_url: ${{ secrets.SLACK_WEBHOOK_URL }}
        thread_ts: "1753841968.598019"
        pretext: "STEP2: composite actions ✅ <!channel>"
        author_name: ":github_white: ${{ vars.AUTHOR_NAME }}"
        repository: "${{ vars.REPOSITORY }}"
        message: ":speech_balloon: (適宜変更してください)"
